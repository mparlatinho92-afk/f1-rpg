#!/usr/bin/env bash
# Windows: powershell.exe existiert -> wie bisher. Sonst: pwsh (Linux/Cloud).
if command -v powershell.exe >/dev/null 2>&1; then
    powershell.exe -File "$(dirname "$0")/manage-v.ps1" "$@"
else
    pwsh -File "$(dirname "$0")/manage-v.ps1" "$@"
fi
